{
  "name": "qqqlang",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:font": "(pip install -r requirements.txt || uv pip install -r requirements.txt) && python scripts/build-font.py",
    "build": "npm run build:font && tsc && vite build",
    "build:readme": "cd scripts/readme && npm install && npx playwright install chromium && npx tsx generate-readme.ts",
    "preview": "vite preview",
    "format": "prettier --write \"**/*.ts\"",
    "format:check": "prettier --check \"**/*.ts\""
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "typescript": "^5.3.3",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "@tensorflow/tfjs": "^4.22.0",
    "@types/three": "^0.182.0",
    "fft.js": "^4.0.4",
    "onnxruntime-web": "^1.23.2",
    "three": "^0.182.0"
  }
}
