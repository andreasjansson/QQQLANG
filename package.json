{
  "name": "qqqlang",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:font": "node scripts/build-font.js",
    "build": "npm run build:font && tsc && vite build",
    "preview": "vite preview",
    "postinstall": "cd node_modules/opentype.js && npm install --ignore-scripts && npm run build"
  },
  "devDependencies": {
    "opentype.js": "git+https://github.com/opentypejs/opentype.js.git",
    "typescript": "^5.3.3",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "@types/three": "^0.182.0",
    "fft.js": "^4.0.4",
    "onnxruntime-web": "^1.23.2",
    "three": "^0.182.0"
  }
}
